# Backend API
fastapi==0.111.0
uvicorn[standard]==0.30.0
pydantic==2.7.0

# AI Engine (pure Python, no ML dependencies needed for rule-based engine)
# Optional: add scikit-learn or transformers if extending with ML models

# Database
# SQLite is built-in to Python — no extra package needed

# Testing
pytest==8.2.0
pytest-cov==5.0.0
httpx==0.27.0          # for FastAPI TestClient

# Sample App
flask==3.0.3
prometheus-client==0.20.0

# Utilities
python-dotenv==1.0.1
